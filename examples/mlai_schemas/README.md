# M-Lab reference schemas

Frozen historical `mice` / `exp` + `populate_base`. Reference only — copy into a
lab repo if needed. Not installed or tested by Base-schemas.

These modules are **not** yet an extension of SCENE `Lab` / `Session`. They will
be updated once the shared experiment spine is stable; until then, treat them as
a standalone legacy example, not the target lab-extension pattern.
